# dns_brute.sh
O script lê uma lista de possíveis subdomínios a partir do arquivo subd_h.txt (wordlist não inclusa) e tenta resolvê-los para um domínio principal informado como argumento na execução do script. Apenas os resultados válidos são exibidos, filtrando respostas do tipo NXDOMAIN.
